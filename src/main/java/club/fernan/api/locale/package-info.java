/**
 * Supported response locales for endpoints that accept a {@code locale} query
 * parameter (currently the store stock and cooldown endpoints).
 *
 * @author trq
 * @since 0.1.0
 */
package club.fernan.api.locale;
